# কাজের অগ্রগতি ও পরিকল্পনা (Progress & Plan)

প্রতিটি টাস্ক শুরুর আগে এখানে টাস্কের ব্যাখ্যা, সম্পূর্ণ করার পরিকল্পনা এবং সুবিধা/অসুবিধা লেখা হয়।
প্রতিটি ধাপের পরে "এখন পর্যন্ত যা করা হয়েছে" অংশটি আপডেট হয়।

## এখন পর্যন্ত যা করা হয়েছে

- **Task 2 (LRU Cache):** সম্পূর্ণ। ১৩টি টেস্ট পাস, typecheck ঠিক আছে, দুটি ডেমো চলে। তিনটি output screenshot README-তে যুক্ত।
- **Task 1 (Order Tracking):** UI তৈরি শেষ। lint, typecheck ও build পাস করেছে। 360px ও 390px-এ যাচাই করা হয়েছে, horizontal scroll নেই। README লেখা ও Vercel-এ deploy করা হয়েছে: https://task-1-order-tracking.vercel.app। অর্ডার তালিকায় প্রতিটি অর্ডারের পাশে status আইকন যোগ করা হয়েছে (ট্রাক = পথে, সতর্কতা = দেরি, টিক = পৌঁছেছে, ঘড়ি = ট্র্যাকিং শিগগিরই)। একই আইকন হিরো কার্ড ও টাইমলাইনেও ব্যবহার হয়।
- **Repo:** দুটি আলাদা git repo-কে একটি repo-তে (`mukim1/vecosoft-assessment`) একত্র করা হয়েছে, সব commit history সহ।
- **Task 3:** উত্তর `task-3-algorithm-explanation/ANSWER.md`-এ (১৯৫ শব্দ, চারটি প্রশ্নের জন্য numbered list)।
- **Prompt history:** `PROMPT_HISTORY.md`-এ দুই session-এর সব prompt, হুবহু, session অনুযায়ী ভাগ করা।
- **Task 4:** এখনও শুরু হয়নি।

---

## Task 1 — Order Tracking Screen (৪০ নম্বর)

**টাস্কটি কী:** একটি e-commerce অ্যাপের মোবাইল Order Tracking স্ক্রিন। এখন শুধু "Processing / Shipped / Delivered" লেবেল দেখায়, যা ব্যবহারকারী বুঝতে পারে না। নতুন স্ক্রিনে সহজ ভাষায় বর্তমান অবস্থা, ডেলিভারির টাইমলাইন, আনুমানিক সময়, অর্ডারের সারাংশ, সাপোর্টে যোগাযোগ এবং loading/empty/error অবস্থা থাকতে হবে। তিনটি বিশেষ পরিস্থিতি সামলাতে হবে:
১. **দেরি হওয়া অর্ডার:** আগের ও নতুন তারিখ, কারণ, এবং পরের পদক্ষেপ (নোটিফাই, সাপোর্ট, বাতিল/রিফান্ড)।
২. **Delivered কিন্তু পাইনি:** ডেলিভারির ছবি, চেকলিস্ট (প্রতিবেশী, নিরাপদ জায়গা, ছবি মিলিয়ে দেখা), তারপর রিপোর্ট।
৩. **ট্র্যাকিং এখনও নেই:** "অর্ডার প্রস্তুত হচ্ছে", ট্র্যাকিং কখন আসবে তা দেখানো। কখনও খালি বা ভাঙা দেখাবে না।

**পরিকল্পনা:**
1. `create-next-app` ও `shadcn` দিয়ে প্রজেক্ট তৈরি (Next.js 16, Tailwind v4, TanStack Query)।
2. ডেটার type, mock অর্ডার এবং দেরি (latency) সহ একটি fake service।
3. একটি pure function (`derive-status.ts`), যা অর্ডার থেকে scenario, রং আর লেখা ঠিক করে। কম্পোনেন্ট শুধু সেটা দেখায়।
4. ছোট ছোট কম্পোনেন্ট: StatusHero, Timeline, DelayBanner, DeliveredCard, ReportIssueSheet, OrderSummary, SupportActions।
5. URL-এ order ID দিয়ে প্রতিটি scenario সরাসরি খোলা যায় (`/orders/VS-1002` ইত্যাদি)।
6. lint, typecheck, build, মোবাইল স্ক্রিনশট দিয়ে যাচাই → README → Vercel deploy।

**সুবিধা:**
- সব business logic এক জায়গায় (pure function), তাই সহজে বোঝা ও ব্যাখ্যা করা যায়।
- Fake service-এ আসল delay আছে, তাই loading/error অবস্থা সত্যিকারের, বানানো নয়।
- শুধু `order-service.ts` বদলালেই আসল API যুক্ত করা যাবে।
- প্রতিটি scenario আলাদা লিংকে খোলে, তাই মূল্যায়নকারীর জন্য দেখা সহজ।

**কীভাবে করা হয়েছে এবং কেন (ডেটা কীভাবে প্রবাহিত হয়):**

```
URL /orders/VS-1002
  → page.tsx (Server Component): URL থেকে orderId নেয়, হেডার দেখায়
  → OrderTrackingScreen ("use client"): useOrderTracking(orderId)
      → TanStack Query → order-service.getOrder() (~900ms delay, mock ডেটা)
  → অপেক্ষার সময় Skeleton | error হলে Try again | অর্ডার না পেলে "খুঁজে পাওয়া যায়নি"
  → deriveOrderView(order, now) → view model (scenario, রং, শিরোনাম, ETA, timeline)
  → কম্পোনেন্টগুলো শুধু view model দেখায়
```

- **Scenario নির্ধারণের নিয়ম (`derive-status.ts`):** delivered হলে → `delivered`; carrier নেই → `tracking-pending`; delay লেখা আছে বা ETA পার হয়ে গেছে → `delayed`; বাকি সব → `on-track`। কেন: সব নিয়ম এক জায়গায় থাকলে বুঝতে, বদলাতে ও টেস্ট করতে সহজ।
- **Mock তারিখ "এখন"-এর সাপেক্ষে:** তাই যেকোনো সময় খুললে দেরির অর্ডার দেরিতেই দেখায়।
- **Bottom sheet:** বিস্তারিত দেখা, রিপোর্ট করা ও বাতিলের জন্য, কারণ মোবাইলে এটাই স্বাভাবিক ধরন।
- **Accessibility:** timeline একটি `<ol>`, বর্তমান ধাপে `aria-current="step"`, শুধু রং নয়, সঙ্গে আইকন ও লেখাও থাকে।

**অসুবিধা / ট্রেড-অফ:**
- ডেটা client-side-এ আসে (TanStack Query), তাই প্রথম render-এ skeleton দেখায়, server-side rendering-এর সুবিধা কম।
- Mock ডেটার সময় "এখন" থেকে হিসাব করা, তাই পেজ অনেকক্ষণ খোলা থাকলে সময় একটু পুরনো হতে পারে।
- রিপোর্ট ও বাতিল করা আসলে কোথাও পাঠানো হয় না (mock), শুধু UI flow দেখানো হয়েছে।
- Test লেখা হয়নি (সময় কম), শুধু lint/typecheck/build ও স্ক্রিনশট দিয়ে যাচাই।

---

## Task 2 — LRU Cache (৩০ নম্বর)

**টাস্কটি কী:** নির্দিষ্ট ক্ষমতার (capacity) একটি cache, যেখানে জায়গা ভরে গেলে সবচেয়ে কম সম্প্রতি ব্যবহৃত (Least Recently Used) জিনিসটি সরিয়ে দেওয়া হয়। `get` ও `put` দুটোই O(1) হতে হবে। বোনাস: TTL (নির্দিষ্ট সময় পরে মেয়াদ শেষ)।

**পরিকল্পনা:**
1. `Map` (key → node) + doubly linked list (sentinel head/tail সহ)।
2. `get`: node খুঁজে list-এর সামনে আনা। `put`: নতুন node সামনে, ভরে গেলে শেষেরটা বাদ।
3. TTL: প্রতিটি node-এ `expiresAt`; `get`-এ মেয়াদ শেষ হলে সরানো (lazy expiry)।
4. `node:test` দিয়ে ১৩টি টেস্ট, দুটি ডেমো, README।

**সুবিধা:**
- কোনো runtime dependency নেই, Node সরাসরি `.ts` চালায়।
- Sentinel node থাকায় list-এর কোডে null check নেই, কোড ছোট ও পরিষ্কার।
- সময় (clock) inject করা যায়, তাই TTL টেস্ট নির্ভরযোগ্য।

**অসুবিধা / ট্রেড-অফ:**
- Lazy expiry: মেয়াদ শেষ হওয়া entry কেউ না পড়লে list-এর শেষে না আসা পর্যন্ত জায়গা দখল করে রাখে।
- Miss হলে `-1` ফেরত দেয় (টাস্কের নিয়ম), কিন্তু `-1` নিজেই মান হলে বিভ্রান্তি হতে পারে।

---

## Task 3 — ব্যাখ্যা (১৫ নম্বর)

**টাস্কটি কী:** Task 2-এর LRU cache নিয়ে ১০০–২০০ শব্দের একটি লিখিত উত্তর, যা ফর্মে পেস্ট করতে হবে। চারটি প্রশ্নের জবাব দিতে হবে:
১. কোন data structure ব্যবহার করেছি এবং কেন।
২. সময় (time) ও জায়গার (space) জটিলতা।
৩. কোন ধরনের ব্যবহারে এটি খারাপ কাজ করে।
৪. AI-এর একটি পরামর্শ যা আমি বদলেছি, বাতিল করেছি বা উন্নত করেছি, এবং কেন।

**পরিকল্পনা:**
1. কোড থেকে সত্য তথ্য নিয়ে খসড়া (`ANSWER.md`): Map + doubly linked list; get/put O(1), জায়গা O(capacity)।
2. খারাপ উদাহরণ: capacity-র চেয়ে বড় sequential scan, যেমন capacity 100, কিন্তু 1–101 key বারবার ক্রমানুসারে পড়া। প্রতিটি key দরকারের ঠিক আগেই বাদ পড়ে, তাই hit rate 0%।
3. AI-এর পরামর্শ (সত্য ঘটনা): AI-এর প্রথম সংস্করণে head/tail null হতে পারত, তাই প্রতিটি insert/unlink-এ null check লাগত। আমি sentinel node ব্যবহার করতে বলি। এতে কোড ছোট ও যাচাই করা সহজ হয়।
4. শব্দ গুনে ১০০–২০০-এর মধ্যে রাখা। তারপর নিজের ভাষায় ঠিক করে নেওয়া।

**সুবিধা:**
- প্রতিটি দাবি কোড ও git history দিয়ে প্রমাণ করা যায়।
- চারটি প্রশ্নেরই আলাদা, স্পষ্ট উত্তর থাকে।

**অসুবিধা / ঝুঁকি:**
- খসড়াটা AI-এর লেখা। উত্তরটা নিজের কণ্ঠে হতে হবে, তাই জমা দেওয়ার আগে নিজের ভাষায় পড়ে ঠিক করতে হবে।
- ২০০ শব্দের সীমায় বিকল্প policy (LRU-K, 2Q) নিয়ে বিস্তারিত বলা যায় না।

## Task 4 — Figma (ঐচ্ছিক, ১০ নম্বর)

*শুরুর আগে এখানে পরিকল্পনা যোগ করা হবে।*
